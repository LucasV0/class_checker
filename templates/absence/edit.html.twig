{% extends 'base.html.twig' %}

{% block title %}Edit Absence{% endblock %}

{% block body %}
    <h1 /
{# 
    {{ include('absence/_form.html.twig') }}

    <a href="{{ path('app_absence_index') }}">retour</a> #}
{{ form_start(form) }}
    {{ form_widget(form) }}
    
{{ form_end(form) }}

{% endblock %}
